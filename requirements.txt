# The scanner itself has no runtime dependencies - Python 3.10+ stdlib only.
# Everything below is optional tooling.

# Test runner (the suite also runs standalone: python tests/test_static_scan.py)
pytest>=8.0 ; extra == "dev"

# Used once to extract reference/owasp_llm_top10_v2_text.txt from the OWASP PDF.
# Not needed to run a scan.
pypdf>=4.0 ; extra == "dev"
